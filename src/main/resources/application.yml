spring:
    profiles: default

server:
    port: 8760

eureka:
    instance:
        hostname: localhost-com
        appname: LOCALHOSTCOM
    client:
        registerWithEureka: false
#        fetchRegistry: false
#        serviceUrl:
#            defaultZone: 'http://${eureka.instance.hostname}:${server.port}/eureka/'
#    server:
#        enableSelfPreservation: false
